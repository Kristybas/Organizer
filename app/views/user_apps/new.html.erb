 <div class="banner" style="background-image: linear-gradient(rgba(0,0,0,0.4),rgba(0,0,0,0.4)), url(https://images.unsplash.com/photo-1501556466850-7c9fa1fccb4c?w=600&auto=format&fit=cro[…]3wxMjA3fDB8MHxzZWFyY2h8Nnx8b3JnYW5pemF0aW9ufGVufDB8fDB8fHww);">

    <h3>L'administratif n'a jamais été aussi simple</h3>

  </div>
  <h4>Mes infos </h4>
   <div class="coordonnees" >
    <% if user_signed_in? %>
            <h2>Mes coordonnées actuels</h2>
            <hr class="thick-hr">
            <div class="row">
      <div class="col-md-3">
        <ul>
        <li>
          <i class="fa-regular fa-address-card" ></i>
          <%= "#{current_user.last_name} #{current_user.first_name}" %>
        </li>
          <li><i class="fa-regular fa-user"style= "margin-right: 10px"></i><%= current_user.email %></li>
        </ul>
      </div>
      <div class="col-md-3">
        <ul>
          <li><i class="fa-solid fa-house" style= "margin-right: 10px"></i><%= current_user.address %></li>
          <li><i class="fa-solid fa-phone" style= "margin-right: 10px"></i><%= current_user.phone %></li>
          <li><i class="fa-solid fa-money-check" style= "margin-right: 10px"></i><%= current_user.rib %></li>
        </ul>
      </div>
    </div>
    </div>
     <% end %>

<div class="formulaire">

<div class="info-container">
        <h1 id="titre">Ajouter une Démarche</h1>
    </div>


  <div class="actuelleinfo-container" data-controller="app-form">
      <div class="texte-start">
  <%= simple_form_for @user_app do |f| %>
  <%= f.input :app_id, collection: App.all ,label: false  %>
  <%= f.input :name, placeholder: "Nom",label: false %>
  <%= f.input :phone, placeholder: "Téléphone",label: false %>
  <%= f.input :address, placeholder: "Adresse",label: false %>
  <%= f.input :email, placeholder: "Email",label: false %>
  <%= f.input :rib, placeholder: "RIB",label: false %>
  </div>
  </div>



   <div class="d-flex justify-content-between">
   <div class="start-date">
       <%= f.input :date_prelevement, placeholder: "Date de prélèvement", label: false, as: :date, html5: true %>
   </div>

  <div class="end-date"><%= f.input :date_renouvellement, placeholder: "Date de renouvellement",label: false, as: :date, html5: true %></div>
</div>
  <div class="montant"><%= f.input :montant, placeholder: "Montant",label: false %></div>
  </div>
<div class="d-flex justify-content-center align-items-center">
  <%= f.button :submit, "Ajouter", class: "btn btn-primary" %>
</div>
<% end %>
<%= link_to 'Retour', root_path %>
