 <div class="banner" style="background-image: linear-gradient(rgba(0,0,0,0.4),rgba(0,0,0,0.4)), url(https://images.unsplash.com/photo-1501556466850-7c9fa1fccb4c?w=600&auto=format&fit=cro[…]3wxMjA3fDB8MHxzZWFyY2h8Nnx8b3JnYW5pemF0aW9ufGVufDB8fDB8fHww);">

    <h3>L'administratif n'a jamais été aussi simple</h3>

  </div>

  <div class="coordonnees" >

    <% if user_signed_in? %>
            <h2>Mes coordonnées actuels</h2>
            <hr class="thick-hr">
            <div class="row">
   <div class="boutton d-flex justify-content-end" id="je-change-mes-coordonnees">
      <%= link_to edit_my_profile_path do %>
        <i class="fa-regular fa-pen-to-square"></i>
        <% end %>
    </div>
      <div class="col-md-6">
        <ul>
        <li>
          <i class="fa-regular fa-address-card" ></i>
          <%= "#{current_user.last_name} #{current_user.first_name}" %>
        </li>
          <li><i class="fa-regular fa-user"style= "margin-right: 10px"></i><%= current_user.email %></li>
        </ul>
      </div>
      <div class="col-md-6">
        <ul>
          <li><i class="fa-solid fa-house" style= "margin-right: 10px"></i><%= current_user.address %></li>
          <li><i class="fa-solid fa-phone" style= "margin-right: 10px"></i><%= current_user.phone %></li>
          <li><i class="fa-solid fa-money-check" style= "margin-right: 10px"></i><%= current_user.rib %></li>
        </ul>
      </div>

    </div>
    </div>
     <% end %>



<h1 id="titre">Modifier mes coordonnées</h1>
<%= simple_form_for @user, url: update_my_profile_path, method: :patch do |f| %>

  <% if @user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user.errors.count, "erreur") %> interdite(s) de sauvegarder :</h2>

      <ul>
        <% @user.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <div class="formulaires" data-controller="remove">
   <div class="d-flex justify-content-between" id="center">
    <div class="edit-user">
      <div class="form-group">
        <%= f.input :first_name, label: 'Prénom' %>
      </div>

      <div class="form-group">
        <%= f.input :last_name, label: 'Nom' %>
      </div>

      <div class="form-group">
        <%= f.input :email, label: 'Email' %>
      </div>
    </div>

    <div class="edit-user">
      <div class="form-group">
        <%= f.input :phone, label: 'Téléphone' %>
      </div>

      <div class="form-group">
        <%= f.input :address, label: 'Adresse' %>
      </div>

      <div class="form-group">
        <%= f.input :rib, label: 'RIB' %>
      </div>
    </div>

    <div class="form-group photo-column">
      <%= f.input :photo, label: 'Photo' %>
    </div>
  </div>
    <div id="enregister">
      <%= f.button :submit, value: 'Enregistrer', class: 'submit-button' %>
    </div>
</div>


  </div>
<% end %>
