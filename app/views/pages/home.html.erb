
<%= render 'pages/coordonnées' %>
<div class="search-bar">
  <%= form_with url: root_path, method: :get, local: true do |form| %>
    <%= form.label :query, "Recherche :" %>
    <%= form.text_field :query, placeholder: "Entrez votre recherche" %>

    <%= form.submit "Rechercher", class: "btn btn-primary" %>
  <% end %>
</div>
    <div class="boutton d-flex justify-content-center" id="je-change-mes-coordonnees">
      <%= link_to edit_user_registration_path, class: 'btn btn-flex', style: 'background-color: #FFD18E; color: white;' do %>
        <i class="fa-solid fa-address-card" style="font-size: 30px;"></i>
        Je change mes coordonnées
        <% end %>

  </div>
 <%= render 'user_apps/cards' %>
